# Grafická baseline — DOCTA DESIGNE GDPR

Záväzná kópia brand grafiky. Obnova do `public/`: `npm run restore:graphics`.

Text a obsah **nie sú** v tomto projekte — pozri originál
`94g4txft9m-arch/Hii` tag `fixacia-2026-08-04`.
